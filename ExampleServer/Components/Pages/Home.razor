@* ------------------------------------------------------------------------------------------------------------------ *@
@* Imports
@* ------------------------------------------------------------------------------------------------------------------ *@

@* ------------------------------------------------------------------------------------------------------------------ *@
@* Descriptors
@* ------------------------------------------------------------------------------------------------------------------ *@
@page "/"
@rendermode InteractiveServer
@inject ILucideDataProvider LucideLookupDictionary

@* ------------------------------------------------------------------------------------------------------------------ *@
@* Html
@* ------------------------------------------------------------------------------------------------------------------ *@
<h3>Lucide Icons Example</h3>

<LucideIcon Name="@IconName"/>
<br>
<LiSignature/>
<br>
<button class="btn btn-primary" @onclick="ChangeIcon">Click me to change the first icon</button>

@* ------------------------------------------------------------------------------------------------------------------ *@
@* Code
@* ------------------------------------------------------------------------------------------------------------------ *@
@code {
    private string IconName { get; set; } = "signature";
    private async Task ChangeIcon() {
        var keys = new List<string>(LucideLookupDictionary.IconSvgData.Keys);
        int randomIndex = new Random().Next(keys.Count);

        IconName = keys[randomIndex];
        await InvokeAsync(StateHasChanged);
    }
}